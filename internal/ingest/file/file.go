package file

/*
type FileReaderConfig struct {
	RootPath   string
	FileList   []string
	DirList    []string
	CountLines bool
}

type LogFileReader struct {
	FileInfo

	output        chan types.Message
	consumeCancel context.CancelFunc
}

func (f *LogFileReader) Messages() <-chan types.Message {
	if f.output == nil {
		f.output = make(chan types.Message, 0)
	}
	return f.output
}
*/
